# regent_cache

Shared Cachex-backed cache helpers for Regent Elixir apps.

This package provides a small common cache surface for Phoenix and plain Elixir
apps that need JSON values, strings, counters, sets, health checks, and a
standard child spec.
