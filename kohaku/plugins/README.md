# Kohaku Plugins

Shared Elixir primitives for Kohaku-style plugin packages.

This package defines small host, storage, keystore, asset, and broadcaster
contracts that protocol packages can use without depending on a browser or a
JavaScript runtime.

Run checks from this folder:

```bash
mix test
```
